# daily-todo

개인용 계층형 할 일 관리 웹 앱. 화면에 보이는 이름은 **My What Todo**.
상세 명세는 `docs/todo-app-prd.md`.
`docs/claude-code-prompts.md`는 처음 만들 때 쓴 기록이지 지금의 지시가 아니다 —
거기 적힌 것과 이 파일이 다르면 **이 파일이 맞다.**

## 절대 제약

- 순수 HTML + CSS + JavaScript(ES2020+)만 사용한다.
- **CSP를 약하게 만들지 않는다.** `index.html`의 `Content-Security-Policy`가 이 앱의
  "밖으로 아무것도 보내지 않는다"를 의도가 아니라 규칙으로 만든다. 핵심은
  `connect-src 'none'`이다 — 나중에 누가 주입 구멍을 내더라도 데이터가 나갈 길이 없다.
  그래서 **인라인 `<script>`와 `style=` 속성을 두지 않는다.** 두면 `'unsafe-inline'`을
  열어야 하고, 그 순간 이 규칙이 헐거워진다. 스타일은 CSSOM(`el.style.setProperty`)으로
  건드린다 — 그건 CSP가 막지 않는다. 예외를 더해야 할 것 같으면 먼저 정말 필요한지 따진다.
- npm 패키지, 프레임워크, 빌드 도구, CDN 요청을 **일절 추가하지 않는다**.
- ES 모듈(`type="module"`)을 쓰지 않는다. 모듈 스크립트는 **언제나 지연 실행**이라
  `<head>`에 둬도 문서를 다 읽은 뒤에 돈다. 첫 페인트 전에 고른 테마를 알아야 하는
  `store.js`가 그 시점을 놓치면 화면이 반대 색으로 칠해졌다가 뒤집힌다 (F-14).
  일반 `<script>` 태그로 로드하고 각 파일은 IIFE로 감싼다.
- `innerHTML`을 쓰지 않는다. 텍스트는 반드시 `textContent`로 넣는다.
- `localStorage` 키는 `daily-todo:v1` 하나만 쓴다. **스키마가 바뀌어도 키는 그대로 두고
  안쪽 `version`만 올린다** (현재 4). 예외는 둘뿐이다 — 손상 데이터 이관용
  `daily-todo:v1:corrupted`와, 세션 저장소에 따로 두는 `daily-todo:v1:run`.
- **`requestAnimationFrame`은 배경 탭에서 멈춘다.** 화면에 그리는 일이 아니라 **반드시
  일어나야 하는 일**을 여기 실으면 탭이 숨은 동안 영영 일어나지 않는다. 실제로 두 번
  밟았다 — 외부 변경 따라가기(읽지도 못한 채 남았다)와 오류 문구 넣기(상자만 열리고
  글이 안 들어갔다). 태스크 하나만 건너면 되는 곳에는 `setTimeout`을 쓴다.
- UI 문구는 전부 한글. **한 문장을 끝까지 맺는다** — 명사로 끊거나("5개까지."),
  은유로 줄이지("이깁니다", "잡힙니다") 않는다. 위치는 화면에 보이는 대로 부른다("우측에").
- 소스에 제어 문자를 남기지 않는다. `Map` 키 구분자로 `'\u0000...'` 같은 걸 쓰면
  원시 NUL이 파일에 박혀 `grep`·`file`이 바이너리로 취급하고, 정적 검사가 조용히 통과해버린다.
  `Map`은 `null`을 그대로 키로 받는다.

## 계층 규칙

- `store.js`는 DOM을 모른다. `document`, `window`를 참조하지 않는다.
- `app.js`는 `localStorage`를 직접 만지지 않는다. 반드시 `Store`를 통한다.
- `parse.js`는 순수 함수만 둔다. 부수 효과 금지.
- **완료 전파, 정렬, 집계는 전부 `store.js` 안에서 끝낸다.**
  `app.js`는 `Store`가 돌려준 배열을 순서대로 그리기만 한다.
- 모든 변경 API는 저장에 실패하면 **변경 전체를 되돌리고 `null`을 준다.**
  `app.js`는 `null`을 "저장하지 못했다"로 읽어 알린다. 부분 적용은 없다.

## 데이터

- 저장 형식은 `{ version: 4, rev, theme, sort, pomodoro, categories, todos }`. `todos`는 **평평한 배열 + `parentId`**다.
- 우선순위는 **숫자 0~3이고 0이 가장 높다.** 값 자체가 순위라 가중치 표를 두지 않는다.
  v2까지의 `"high"`/`"normal"`/`"low"`는 로드 시 `0`/`1`/`2`로 옮긴다.
- 테마 선택과 뽀모도로 회차 설정도 이 안에 넣는다. 별도 `localStorage` 키를 만들지 않는다.
- 다만 **1초마다 바뀌는 값은 저장 형식에 넣지 않는다.** 판 번호가 계속 올라가 다른 탭이
  그때마다 다시 읽고, 그 탭이 고치던 것과 되돌릴 수 있던 5초가 함께 날아간다.
  뽀모도로는 **설정만** 이 안에 넣는다.
- 돌아가는 뽀모도로는 **세션 저장소**의 `daily-todo:v1:run`에 따로 둔다. 새로고침을
  넘기되 탭을 닫으면 사라지고, 판 번호를 건드리지 않아 다른 탭이 눈치채지 못한다.
  내보낸 파일에도 들어가지 않는다 — 끝나는 시각은 남의 기기에서 이미 지난 시각이다.
  **여기에도 1초마다 쓰지 않는다.** 남은 초는 끝나는 시각에서 다시 나오므로
  "지금 상태" 요약에서 빼고, 요약이 달라질 때만 쓴다.
- **카테고리는 고정값이 아니다.** 기본 3종(`work`/`personal`/`study`)으로 시작하지만
  사용자가 만들고 지우고 **이름을 바꾼다**. 코드에 카테고리 id나 이름을 하드코딩하지 말고
  `Store.getCategories()`를 쓴다. 항목은 이름이 아니라 id로 카테고리를 가리킨다 —
  그래서 이름을 바꿔도 소속과 색조가 그대로 남는다.
- 카테고리 색은 `hue` 하나만 저장한다. 채도·명도는 `styles.css`가 테마별로 정한다.
  완성된 색상값을 데이터에 넣지 않는다 — 라이트·다크 두 벌이 어긋난다.
- **카테고리는 64개까지다** (`Store.MAX_CATEGORIES`). 상한이 없으면 색조를 고르는
  `Math.min(...used)`가 스프레드 인자 개수 제한에 걸려 `RangeError`로 앱을 멈춰 세운다.
  만드는 자리만 막지 말고 **로드·가져오기에서도 같은 자리에서 자른다** — 만들 때만 막으면
  다음에 앱을 열 때 카테고리가 예고 없이 사라지고 항목만 남는다. 이름 중복도 세 곳 모두에서 막는다.
- **미분류는 없다.** 항목은 언제나 어떤 카테고리에 속하고, 마지막 카테고리는 지울 수 없다.
- **하위의 카테고리는 상위를 통해서만 바뀐다.** `update`가 하위의 `category` patch를 거절한다.
  따로 바꿔주면 `countInCategory`는 그 카테고리로 세는데 카테고리 필터는 상위 기준이라
  걸리지 않고, 다음 로드에서 `renumber`가 상위 값으로 조용히 되돌린다.
- 깊이는 2단계로 **UI에서만** 막는다. 스키마는 임의 깊이를 표현할 수 있다.
- 필터는 `{ type, value, query }`다. `type`(전체/카테고리/태그)은 하나만 켜지지만
  `query`(검색어)는 그 위에 함께 얹힌다. 태그 필터 자동 복귀를 판정할 때는
  **검색어를 뺀 채로** 물어야 한다.
- **태그 바는 태그 축을 뺀 채로 센다.** 지금 고른 필터로 그대로 세면 그 태그 하나만 남아
  다른 태그로 건너뛸 길이 사라진다. 카테고리와 검색어는 그대로 태그 바를 좁힌다.
- **정렬은 어느 모드에서도 형제 그룹 안에서만** 한다. 순서를 바꿔도 `parentId`는 건드리지 않는다.
  부모가 바뀌면 캐스케이드 범위·진행률 분모·완료 전파가 한꺼번에 흔들린다.
- 조회는 `Map` 색인(`byId`, `parentId → 자식들`)을 쓴다. 배열을 항목마다 훑으면
  질의가 O(n²)이 된다. **구조를 바꾼 자리마다 `invalidate()`를 부른다** —
  빠뜨리면 새로 넣은 하위가 부모에게 안 보인다.
- 저장본을 열 때와 파일을 가져올 때는 **같은 검증 파이프라인**(`adopt`)을 탄다.
  가져온 파일이 앱을 무너뜨릴 수 없어야 한다.
- **손상은 파싱 실패만이 아니다.** 파싱은 되는데 우리 형식이 아닌 것(객체가 아니거나
  `todos`가 배열이 아닌 것)도 같은 경로로 보낸다. 여기서 그냥 빈 목록으로 시작하면
  배너도 백업도 없이 멀쩡한 새 앱처럼 보이고, 원본은 저장소에 남아 있다가 항목 하나를
  추가하는 순간 예고 없이 덮인다.
- 저장은 **덮어쓰기가 아니다.** 블롭에 판 번호(`rev`)가 있고, 쓰기 직전에 저장소를
  다시 읽어 우리가 본 판과 같을 때만 쓴다. 다르면 다른 탭이 먼저 쓴 것이므로 물러난다.
  `storage` 이벤트로 따라가는 것만으로는 경합을 막지 못한다.
- **판 번호를 못 읽는 저장본도 그냥 덮지 않는다.** 판 번호를 넣기 전에 쓰인 v1·v2 블롭이
  여기 해당한다. 비교할 판이 없을 때는 **우리가 읽어온 그 원문 그대로인가**로 가른다 —
  전부 충돌로 보면 옛 저장본을 가진 사람이 영영 저장하지 못하고, 전부 통과시키면
  남의 저장본을 날린다.
- 되돌릴 것을 쥔 채로 상태를 갈아치우는 API는 **스냅샷에 전부 담는다.** 하나라도 빠지면
  "되돌렸습니다"라고 알리면서 그 값만 새것으로 남는다 (`importData`의 `theme`·`sort`·`pomodoro`).

## 스타일

- 라이트·다크 팔레트를 두 벌 적지 않는다. 값마다 `light-dark(라이트, 다크)`를 쓰고
  테마는 `color-scheme` 하나로 뒤집는다. 한쪽만 고치는 실수가 구조적으로 불가능해진다.
- **`store.js`는 `<head>`에서 읽고, 나머지 스크립트는 본문 끝에 둔다.** 고른 테마가
  첫 페인트 전에 정해져야 반대 색으로 칠해졌다가 뒤집히지 않는다. `<head>`에서는
  `Store.peekTheme()`만 부른다 — `Store.load()`를 부르면 손상 데이터를 치워버려
  나중에 알림을 띄울 기회가 사라진다.
- **1초마다 도는 자리에서는 바뀐 것만 쓴다.** 같은 문자열을 다시 넣어도 텍스트 노드는
  통째로 갈리고, 개수가 고정된 목록을 매번 헐고 다시 세우면 바뀐 것이 없는 초에도
  DOM이 계속 흔들린다. `classList.toggle(name, 값)`은 이미 그 상태면 속성을 건드리지 않는다.
- **글자 대비는 라이트·다크 양쪽에서 4.5:1을 지킨다.** 평면 배경만 재고 넘어가지 않는다 —
  같은 색이 틴트 위에도 얹히고, 거기서 기준 아래로 내려가는 일이 실제로 있었다.
  색조가 도는 값(카테고리 배지)은 밝기를 `oklch()`로 못박는다. `hsl`의 명도는 같은 값이라도
  색조마다 휘도가 달라 절반이 미달한다. 색조는 `oklch(from hsl(...) L c h)`로 물려받는다 —
  두 좌표계의 각도는 같은 숫자라도 다른 색이라, `hue`를 `oklch()`에 바로 넣으면
  카테고리끼리 색이 붙어버린다.
- `opacity`로 글자를 흐리게 하지 않는다. 겹치면 대비가 그만큼 떨어지고, 얼마나 떨어졌는지는
  계산해보기 전에는 보이지 않는다. 색을 직접 고르고 크기로 구분한다.
- **`role="alert"` 영역은 숨김을 푸는 것과 글을 채우는 것을 한 태스크에 넣지 않는다.**
  그러면 브라우저가 "글을 가진 영역이 통째로 새로 생겼다"고 보아 일부 스크린리더가 읽지 않는다.
  두 줄의 순서를 바꾸는 것으로는 달라지지 않는다. 자리를 빈 채로 먼저 열고 글은
  `setTimeout`으로 다음 태스크에 넣는다.
- **편집기가 포커스를 잃어 끝났을 때는 그 자리에서 다시 그리지 않는다.** 마우스는
  mousedown → focusout → mouseup → click 순서로 도는데, focusout에서 목록을 통째로
  다시 세우면 mousedown을 받은 버튼이 떨어져 나가 이어질 click이 갈 곳을 잃는다.
  편집을 끝내려고 누른 그 첫 클릭이 삼켜지는 것이다. 손을 뗄 때까지 기다렸다가 그린다.
- **다시 그리면 사라지는 입력 내용은 밖에 받아둔다.** 하위 입력창은 매 렌더마다 새로
  만들어지므로, 받아두지 않으면 다른 항목을 체크하는 순간 치던 글자가 빈 문자열이 된다.
- 색은 카테고리 · 우선순위 accent · 중립 배경까지다. 그 밖에 색을 늘리지 않는다.
  아이콘도 `currentColor` 한 가지로 그린다 — 모양으로 알아보게 만든다.
  예외는 뽀모도로 토마토(`--tomato`) 하나뿐이다. 예외를 늘리려면 PRD에 이유를 적는다.
- 소리가 필요하면 Web Audio로 그 자리에서 만든다. 음원 파일을 두지 않는다.
- 확인이 필요한 곳에는 `<dialog>` + `showModal()`을 쓴다. `alert`/`confirm`/`prompt`는
  스타일을 맞출 수 없고 자동화 도구에서 페이지를 멈춰 세운다.
- **되돌릴 것이 걸려 있는 동안에는 알림이 토스트 자리를 뺏지 않는다.** 알림은 뒤에 다시
  띄우면 되지만, 지운 항목은 그 5초가 지나면 영영 되살릴 수 없다. 뽀모도로처럼
  사용자가 누르지 않아도 뜨는 알림이 있으므로 이 순서를 코드로 지켜야 한다.
- 그래서 **지운 뒤에는 `showUndo()`를 먼저 부르고 `render()`를 나중에 부른다.** 위 규칙은
  이미 걸린 것이 있을 때만 걸리는데, 필터 자동 복귀 알림은 `render()` 안에서 나온다.
  뒤집으면 그 알림이 토스트를 차지했다가 실행 취소 토스트에 곧바로 덮여 사라진다.
  **지우는 길이 둘이므로 두 곳 모두에 건다** — 한 건 삭제와 완료 항목 일괄 삭제다.
- 무엇이 몇 초마다 바뀌는지와 `transition` 시간을 따로 정하지 않는다. 전환이 갱신 주기보다
  짧으면 그만큼만 움직이고 멈춰 있어, 이어서 흐르지 않고 끊겨 보인다.
- `prefers-reduced-motion: reduce`는 `*`로 한 번에 덮는다. 전환마다 선택자를 적어 끄면
  나중에 하나를 빠뜨리고, 빠뜨렸다는 사실은 그 설정을 켠 사람만 알게 된다.
  이 파일에서 `!important`는 그 한 곳뿐이다 — 어느 규칙보다 뒤에 서야 하기 때문이다.
- 끌어 놓기를 붙이면 **키보드 대안도 같이 붙인다**. 마우스가 없으면 못 하는 동작을 남기지 않는다.
- hover로만 드러나는 버튼은 반드시 `@media (hover: hover) and (pointer: fine)` 안에서 숨긴다.
  터치 화면에는 hover가 없어, 밖에서 숨기면 그 기능이 영영 사라진다.
- 날짜를 파일명이나 화면에 쓸 때 `toISOString()`을 쓰지 않는다. UTC라 한국 오전에는 하루 전이다.
- **서로 맞물리는 치수는 한쪽에서 유도한다.** 하위 들여쓰기를 연결선 위치와 따로 적어두면
  한쪽만 고쳤을 때 선이 하위 내용 위로 올라탄다 — 실제로 선이 우선순위 숫자를 스치고 있었다.
  좁은 화면 재정의가 있으면 거기서도 같은 실수가 난다.
- **칸의 가운데에 놓을 것에는 한쪽 패딩을 주지 않는다.** `text-align: center`는 패딩 안쪽의
  가운데를 잡으므로 그만큼 밀린다. 자리를 비워둘 목적이면 `position: absolute`로 흐름에서 빼낸다.
- **개수에 상한을 두면 그 개수를 화면에 그려본다.** 데이터 상한만 두고 넘어가면
  "만들 수는 있는데 만들면 무너지는" 수가 남는다. 실제로 카테고리 64개를 허용해두고
  탭 바·태그 바·관리 목록 셋 다 상한 없이 자라게 두어, 43개에서 목록이 화면 밖으로
  밀려나고 관리 패널이 1920px로 펼쳐졌다. **극단에서만 나는 일이 아니다** — 폰 폭에서는
  카테고리 8개에 벌써 톱니가 첫 줄에서 39px 내려갔다. 늘어나는 목록에는 높이 상한과
  `overflow-y: auto`를 함께 준다.
- **줄이 늘어나는 블록 옆에 도구를 둘 때는 `align-items: center`를 쓰지 않는다.**
  줄이 늘어날 때마다 도구가 블록 한복판으로 내려가 그 안에 끼어 있는 것처럼 보인다.
  위에 붙이고, 한 줄과 도구의 높이 차이만큼만 내려 첫 줄 가운데에 세운다.

## 작업 규칙

- 각 단계가 끝나면 로컬 서버로 띄워 동작하는 상태여야 한다.
- 요청하지 않은 기능을 추가하지 않는다. PRD의 "v2 후보" 목록은 손대지 않는다.
- 기존 단계에서 만든 동작을 깨뜨리지 않는다.
- 명세와 다르게 구현했다면 PRD §13에 이유와 함께 남긴다.

## 확인 방법

`python3 -m http.server`로 띄워 확인한다. 테스트 프레임워크는 설치하지 않는다.

`file://`로 여는 것은 **더 이상 지원 대상이 아니다.** 온라인에 올려 그 주소로 들어간다.
그래도 빌드 도구는 여전히 두지 않는다 — 정적 파일 그대로 올린다.

## 배포

**저장소 하나만 관리한다.** `perust/my-what-todo`(공개)가 GitHub Pages로 직접 서비스된다.

```
perust/my-what-todo   main에 push → Pages가 그대로 내보낸다
                      └ https://perust.github.io/my-what-todo/
perust/perust.github.io   작업 페이지에서 그 주소를 링크만 한다
```

- **앱을 고쳤으면 이 저장소에만 올린다.** 블로그는 건드릴 것이 없다.
  예전에는 블로그가 앱 파일을 사본으로 들고 있어서 둘 다 올려야 했고,
  실제로 그것을 잊어 **몇 주 묵은 버전이 올라가 있었다** — 그 사이 사이트에는
  데이터가 말없이 사라지는 결함 셋이 살아 있었다. 사본을 없앤 이유다.
- `.nojekyll`을 지운다거나 하지 않는다. Pages의 Jekyll 처리를 꺼둔 것이라,
  없으면 `_`로 시작하는 이름이 조용히 빠지는 규칙을 하나 더 외워야 한다.
- 스크립트와 스타일은 **상대 경로**로 건다. 저장소 이름이 바뀌면 주소의 경로도
  바뀌므로, 절대 경로로 적으면 그때 전부 깨진다.
- 배포가 끝나면 **실제 주소로 들어가 확인한다.** 로컬에서 되는 것과 별개다.
  이때 **반드시 캐시를 지우고 새로 받는다**(`Cmd+Shift+R`). Pages가 모든 파일에
  `max-age=600`을 주므로, 주소 끝에 `?무엇`을 붙이는 것만으로는 `index.html`만 새로 받고
  `app.js`·`styles.css`는 캐시에 있던 것이 그대로 온다. **새 화면에 옛 동작이 얹혀
  실제로 배포가 안 된 것처럼 보인다** — 로그인 대화상자를 올린 날 그렇게 읽을 뻔했다.
  주소에 버전 쿼리(`app.js?v=5`)를 다는 길도 있지만 붙이지 않는다. 그건 인라인 스크립트의
  CSP 해시를 피하려고 `theme.js`를 파일로 뺀 이유 — 잊으면 조용히 깨지는 수동 갱신 —
  를 그대로 다시 들이는 일이다. 확인하는 사람이 한 번 더 누르는 편이 싸다.

> **저장소 이름을 바꿀 때**: Pages 주소가 함께 바뀐다. 블로그 작업 페이지의 링크와
> `README.md`의 clone 주소를 같이 고쳐야 한다. 저장된 할 일은 출처(`perust.github.io`)가
> 그대로라 경로가 바뀌어도 이어진다 — `localStorage`는 출처 기준이고 경로를 보지 않는다.

> **저장소를 나눠도 데이터는 나뉘지 않는다**: 브라우저는 저장소를 모른다. 출처는
> `프로토콜 + 호스트 + 포트`뿐이라 경로도 저장소도 들어가지 않는다. 그래서
> `perust.github.io` 아래의 모든 프로젝트가 **같은 저장소 서랍을 공유한다** —
> 확인해보니 이 앱의 `daily-todo:v1` 옆에 퀴즈·게임·손글씨 인식기의 키가 함께 있었다.
> 어느 페이지에서든 서로의 데이터를 읽고 고칠 수 있다.
>
> **지금은 그대로 둔다.** 그 서랍을 여는 코드가 전부 직접 만든 것이기 때문이다.
> 조건이 바뀌는 때는 **남의 코드를 들일 때** — 댓글 위젯·방문자 분석·폰트 CDN 같은 것을
> 블로그에 붙이는 순간이다. 그때 그 스크립트는 할 일도 게임 세이브도 다 볼 수 있다.
> CSP는 여기에 도움이 되지 않는다. 내 문서에만 적용되므로 다른 페이지의 스크립트는
> 자기 문서의 규칙을 따른다. 나누려면 별도 도메인뿐이고, 그때 기존 데이터는
> 따라오지 않으므로 내보내기/가져오기로 옮겨야 한다.

